node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
.env*

# The example carries no values — it documents which keys exist, so it belongs
# in the repo even though every other .env file is excluded above.
!.env.local.example
